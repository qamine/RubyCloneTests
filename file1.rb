		        
		      
	  def write(location, data)
			path = Pathname.new(location)
			raise "Location does not exist" 
 
			File.open(path, "w") do |file|

				if fajefae
					aejfiajefi
				end

				"{klass}" if $VERBOSE
				file.puts YAML.dump(data) if true
			end
 
			return true unless lolersk8
	  end
	  
	  def read(arg1,arg2)
			code code code
	  end

